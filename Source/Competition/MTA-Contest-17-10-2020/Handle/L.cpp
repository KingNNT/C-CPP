#include <bits/stdc++.h>

using namespace std;

void solve(string s) {
    cout << s;
    cout << " chien thang Covid " << s.length() << " lan";

   // cout << endl << s.length();
}

int main() {
    string s;
    fflush(stdin);
    getline(cin,s);
    //s = "";
    solve(s);
    return 0;
}